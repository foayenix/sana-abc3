name: sana_algorithms_ui
description: SANA Health Framework Algorithm Testing Dashboard
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # HTTP & State Management
  http: ^1.1.0
  provider: ^6.1.0

  # UI Components
  fl_chart: ^0.65.0

  # Utilities
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
